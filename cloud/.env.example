# Example environment for Cloudflare Worker local dev / docs only.
# Never commit real secrets. For production ingestion, use Wrangler secrets or
# GitHub Actions encrypted secrets (see docs/operations/CI_CD.md).

# Bearer token the Worker expects on POST /ingest (must match GitHub Action secret INGESTION_SECRET)
INGESTION_SECRET=
